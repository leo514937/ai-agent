# 5. 文件修改清单

> 本次任务只做“规划文档整改”，不修改业务代码、不新增 Python/Java 实现、不改测试代码。

---

## 5.1 本次实际修改的 md 文档

| 文件 | 修改类型 | 关键整改点 |
|---|---|---|
| `todo/arch_refactor/03_最小改造方案.md` | 大幅整改 | 收紧 P0 边界，新增 `LocalLifeGoalDraft`、Candidate/Goal 枚举、`candidate_limit=None` 策略、fallback 追踪约束，并明确 P0 `next_action` 只允许 `finish / clarify / degrade_answer / fallback` |
| `todo/arch_refactor/05_文件修改清单.md` | 重写 | 重新整理本次文档整改与后续代码实现建议的映射 |
| `todo/arch_refactor/06_风险与回归.md` | 重写 | 增加 dangling `next_action`、旧 `task_type` 继续绑架、Candidate 状态自由字符串、fallback 静默覆盖、P0 只解决候选不解决事实、非 local_life 污染、新规则堆叠等风险 |
| `todo/arch_refactor/07_测试计划.md` | 重写 | 按 P0/P1/P2 分层边界重构测试计划，增加 `LocalLifeGoalDraft`、枚举、fallback 追踪、非 local_life 直达和 trace 断言要求 |
| `todo/arch_refactor/08_实施顺序.md` | 重写 | 重新整理文档阶段、P0、P1、P2 的实施纪律与门禁，强调不允许一次性跨阶段重构 |
| `todo/arch_refactor/09_Review_Observe_Sufficiency_Check设计.md` | 重写 | 作为 Review 主设计文档，补充 `LocalLifeGoalDraft`、Candidate 枚举、P0 `next_action` 边界、fallback 追踪、阶段验收边界 |

---

## 5.2 每个文件新增或纠偏的关键章节

### `03_最小改造方案.md`

1. P0 的真实目标与非目标
2. P0 的 `next_action` 真实边界
3. `LocalLifeGoalDraft`
4. Candidate / Goal / Review 状态枚举
5. `candidate_limit=None` 默认策略
6. `slot_extractor` fallback 边界
7. 非 local_life 直达边界

### `06_风险与回归.md`

1. P0 输出未实现 `next_action`
2. 旧 `task_type` 继续作为最高事实源
3. Candidate 状态自由字符串
4. `slot_extractor` fallback 静默覆盖 LLM 语义
5. P0 只解决候选、不解决证据
6. P1 未及时落地仍继续产生事实错误
7. 非 local_life 被 CandidateSet 污染
8. 一次性重构 P0/P1/P2
9. `_h_target_resolve` 规则堆叠搬家到 `CandidateResolver`

### `07_测试计划.md`

1. `LocalLifeGoalDraft / 枚举 / fallback` 测试
2. CandidateReview 测试
3. EvidenceReview 测试
4. DecisionReview 测试
5. AnswerVerifier 测试
6. Retry Policy 测试
7. 非本地生活直达测试
8. P0 / P1 / P2 验收边界

### `08_实施顺序.md`

1. 第一轮只改文档
2. 第二轮只实现 P0
3. P0/P1/P2 阶段门禁
4. 每阶段先单测再端到端
5. 必须断 `trace`
6. bug 分类定位纪律

### `09_Review_Observe_Sufficiency_Check设计.md`

1. 轻量 `LocalLifeGoalDraft`
2. Candidate / Goal 枚举设计
3. `candidate_limit` 默认值策略
4. `slot_extractor` fallback 边界
5. P0 的 `next_action` 边界
6. P0/P1/P2 阶段验收边界

---

## 5.3 后续代码实现建议文件

### P0 优先

1. `planning/review_policy.py`
2. `planning/candidate_review.py`
3. `target/candidate_resolver.py`
4. `domain` 中 Candidate / Goal / ToolResult 相关 schema 文件
5. `engine/graph_builder.py` 中 `_h_target_resolve` 的薄调度改造
6. `semantic/intent_parser.py` 与 `semantic/slot_extractor.py` 的字段边界与 fallback 追踪

### P1 优先

1. `planning/evidence_planner.py`
2. `planning/evidence_review.py`
3. `planning/facet_planner.py`
4. `planning/comparison_planner.py`

### P2 优先

1. `planning/goal_planner.py`
2. `planning/decision_planner.py`
3. `planning/decision_review.py`
4. `domain/state.py`

---

## 5.4 当前边界结论

本次整改后，P0/P1/P2 的边界已基本清晰：

1. P0 只解决候选发现与候选 sufficiency，不做完整 Replan
2. P1 才解决 `unknown_as_false / failed_as_empty`
3. P2 才让 `GoalPlanner / DecisionPlanner / DecisionReview` 真正接管旧硬分流

仍需注意的一点是：

`04_完整改造方案.md` 仍保留的是完整目标架构视角，不应被误读为 P0 当期实现范围。
